# Sparse C64 PRG for the virtual-1541 LOADFIRST smoke loader.
# The PRG itself starts at $0801, but the real loader code lives at $C000 so it
# can safely load another BASIC PRG over $0801.
MEMORY {
    LOADADDR: start = $07FF, size = $0002, file = %O;
    ZP:       start = $0002, size = $001E, type = rw;
    BASIC:    start = $0801, size = $0010, file = %O, fill = yes;
    HIGH:     start = $C000, size = $2000, file = %O;
}

SEGMENTS {
    LOADADDR: load = LOADADDR, type = ro;
    ZEROPAGE: load = ZP,       type = zp;
    STARTUP:  load = BASIC,    type = ro;
    CODE:     load = HIGH,     type = ro;
    RODATA:   load = HIGH,     type = ro;
    BSS:      load = HIGH,     type = bss, define = yes;
}
