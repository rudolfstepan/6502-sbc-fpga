MEMORY {
    HEADER: start = $0000, size = $0002, file = %O;
    RAM:    start = $0200, size = $0200, file = %O;
}

SEGMENTS {
    LOADADDR: load = HEADER, type = ro;
    CODE:     load = RAM,    type = ro;
    RODATA:   load = RAM,    type = ro;
}
