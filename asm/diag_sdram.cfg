MEMORY {
    ROM:     start = $C000, size = $3FFA, fill = yes, fillval = $EA, file = %O;
    VECTORS: start = $FFFA, size = $0006, fill = yes, fillval = $EA, file = %O;
}

SEGMENTS {
    CODE:    load = ROM,     type = ro;
    VECTORS: load = VECTORS, type = ro;
}
