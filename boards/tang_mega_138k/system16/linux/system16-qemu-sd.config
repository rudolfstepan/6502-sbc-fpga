# QEMU virt exposes the root filesystem as a virtio-mmio block device.
# The board-specific Gowin host remains enabled only in the hardware build.
# CONFIG_GORV32_SD is not set
CONFIG_VIRTIO=y
CONFIG_VIRTIO_MMIO=y
CONFIG_VIRTIO_BLK=y
