# Flash-resident rescue shell plus the board SD block driver.  The SD card is
# a read-only test device and is never mounted by this safety profile.
CONFIG_BLOCK=y
CONFIG_GORV32_SD=y
# CONFIG_EXT2_FS is not set
