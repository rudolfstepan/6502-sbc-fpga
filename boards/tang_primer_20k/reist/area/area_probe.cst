// Pins for the area/Fmax probe tops (reist_reduce_top / ip_reduce_top).
// Only a clock and one observable output are needed.
IO_LOC  "clk" H11;
IO_PORT "clk" IO_TYPE=LVCMOS33 PULL_MODE=NONE;
IO_LOC  "probe" L16;
IO_PORT "probe" IO_TYPE=LVCMOS33 PULL_MODE=UP DRIVE=8;
